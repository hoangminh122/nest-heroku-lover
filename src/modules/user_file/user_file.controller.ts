import { Body, Controller, Delete, Get, Param, Post, Put } from "@nestjs/common";
import { ApiProperty, ApiTags } from "@nestjs/swagger";

@Controller('user_file')
@ApiTags('UserFile')
export class UserFileController {
    
}